# Utils : Generic Utilities

This module serves as the base of the application, containing utilities used by all other modules. The rules for this module are:
 * Nothing from this module may depend on any other module
 * Avoid circular dependencies
 * Reduce intra-modular dependencies where possible
 
